-   model: expenses.CategoryGroup
    pk: 1
    fields:
        name: group


-   model: expenses.Category
    pk: 1
    fields:
        name: groceries
        group: ['group']
        is_negative: true

-   model: expenses.Category
    pk: 2
    fields:
        name: STUFF
        group: ['group']
        is_negative: true

-   model: expenses.Category
    pk: 4
    fields:
        name: Inactive
        group: ['group']

-   model: expenses.Category
    pk: 3
    fields:
        name: other
        group: ['group']

-   model: expenses.Category
    pk: 5
    fields:
        name: positive 
        group: ['group']
        is_negative: false