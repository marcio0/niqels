function InstallmentFormCtrl ($scope) {
    "use strict";

    $scope.is_installment = false;

    $scope.selectInstallment = function selectInstallment () {

    };
}

InstallmentFormCtrl.$inject = ['$scope'];